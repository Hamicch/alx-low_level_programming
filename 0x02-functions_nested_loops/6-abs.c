#include "holberton.h"

/**
 * _abs - COmputes the absolute value of an integer
 *
 *@a: The integer to be computed
 *
 * Return: Absolute value
 */

int _abs(int a)
{	
	if (n >= 0)
		return (n);
	else
		return (n * -1);
}
